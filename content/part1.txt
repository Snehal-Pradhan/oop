# Part 1 — Introduction to OOP

## 1. Java Basics (Quick Refresher)

### Hello World
```java
public class Main {
    public static void main(String[] args) {
        System.out.println("Hello World");
    }
}
```
- **public**: accessible from anywhere
- **static**: no object needed to run
- **void**: no return value
- **String[] args**: command-line arguments

### Comments
```java
// single line
/* multi-line */
```

### 8 Primitive Data Types
| Type | Size | Range |
|------|------|-------|
| `byte` | 1B | -128 to 127 |
| `short` | 2B | ±32K |
| `int` | 4B | ±2B (most common) |
| `long` | 8B | large ints |
| `float` | 4B | decimals (suffix `f`) |
| `double` | 8B | decimals (default) |
| `char` | 2B | single char `'A'` |
| `boolean` | 1 bit | `true`/`false` |

### Operators
- **Arithmetic**: `+ - * / %`
- **Unary**: `++ -- !`
- **Relational**: `== != > < >= <=`
- **Logical**: `&& ||`
- **Assignment**: `= += -= *= /= %=`

### Strings (Immutable)
```java
String s1 = "Hello";
String s2 = new String(new char[]{'W','o','r','l','d'});
s1 + " " + s2;           // concatenation
s1.charAt(1);            // 'e'
s1.length();             // 5
s1.substring(0, 2);      // "He"
s1.equals("Hello");      // true (content comparison)
```

### Input (Scanner)
```java
import java.util.Scanner;
Scanner sc = new Scanner(System.in);
int age = sc.nextInt();
String name = sc.next();
sc.close();
```

### Type Casting
- **Implicit** (widening): `int → double` (automatic)
- **Explicit** (narrowing): `double → int` (manual, truncates)

### Constants
```java
final float PI = 3.14f;  // cannot reassign
```

### Arrays
```java
int[] scores = {90, 80, 70};
scores.length;        // 3
scores[0];            // 90

// For-each
for (int i : scores) { ... }

// 2D
int[][] matrix = {{1,2}, {3,4}};
```

### Conditionals
```java
if (marks > 90) { ... }
else if (marks > 80) { ... }
else { ... }

switch (day) {
    case 1: System.out.println("Mon"); break;
    case 2: System.out.println("Tue"); break;
    default: System.out.println("Invalid");
}
```

### Loops
```java
for (int i = 0; i < 5; i++) { ... }
while (i < 5) { ...; i++; }
do { ...; i++; } while (i < 5);
```

### Exception Handling
```java
try {
    int[] nums = {1,2,3};
    System.out.println(nums[10]);
} catch (Exception e) {
    System.out.println("Error");
} finally {
    System.out.println("Always runs");
}
```

---

## 2. Object-Oriented Programming (OOP)

**OOP** is a paradigm based on **objects** containing data (attributes) and code (methods).

- **Class**: blueprint/template defining properties & behaviors
- **Object**: instance of a class with its own state

### Procedural vs OOP

| Aspect | Procedural | OOP |
|--------|-----------|-----|
| Approach | Step-by-step actions | Model real-world entities |
| Data | Globally accessible | Encapsulated in objects |
| Reusability | Limited | High (inheritance, polymorphism) |
| Scalability | Harder | Scales better |
| Modularity | Functions (loose) | Classes (structured) |

### Why OOP for large apps?
- **Modularity**: break into Account, Customer, Transaction classes
- **Reusability**: Vehicle → Car/Bike via inheritance
- **Scalability**: add features without modifying existing code
- **Security**: encapsulate sensitive data (e.g., `private balance`)

---

## 3. Class & Object

### Class Definition
```java
class Employee {
    private int salary;
    public String employeeName;

    public void setName(String s) { employeeName = s; }
    public void setSalary(int val) { salary = val; }
    public int getSalary() { return salary; }
}
```

### Object Creation
```java
Employee obj1 = new Employee();
obj1.setName("Raj");
obj1.setSalary(10000);

Employee obj2 = new Employee();
obj2.setName("Rahul");
```

### Key Points
- Class = blueprint (no memory allocated)
- Object = instance (memory on heap via `new`)
- Each object has its own memory — cannot access another object's data directly

### Object Deletion (Java)
```java
Employee obj = new Employee();
obj = null;  // eligible for GC
```
- Java uses **Garbage Collector** — automatic when no references remain
- Stack: primitives + object references
- Heap: all objects created with `new`

### Attributes & Methods

**Attributes** = data/fields (state of object)
**Methods** = behavior/operations on that data

Example — `BankAccount`:
```java
class BankAccount {
    private String name;
    private double balance;

    public BankAccount(String name, double balance) {
        this.name = name;
        this.balance = balance;
    }

    public void setName(String name) { this.name = name; }
    public String getName() { return name; }
    public double getBalance() { return balance; }

    public void deposit(double amount) { balance += amount; }

    public boolean withdraw(double amount) {
        if (amount > balance) { return false; }
        balance -= amount;
        return true;
    }
}
```

**Getters & Setters**: controlled access to private attributes
- Getters: retrieve value
- Setters: modify value (often with validation)

### Important Pickles
- Keep attributes `private`, expose via public methods (encapsulation)
- In Java, class fields get default values (0, null, false)
- Local variables inside methods — must be explicitly initialized
- Validate inputs in setters (no negative deposits, insufficient balance, etc.)

---

## 4. Constructors

Special method automatically called on object creation.

### Rules
- Name = class name
- No return type (not even `void`)
- Runs automatically at `new`
- If no constructor defined → compiler generates **default constructor**

### Default Constructor Behavior
- Generated only if no explicit constructor exists
- Calls `super()` implicitly
- Instance variables get default values: `0`, `0.0`, `null`, `false`
- Local variables — NOT initialized (compiler error if used before init)

```java
class Employee {
    int id;       // 0 (default)
    double salary; // 0.0 (default)
    String name;  // null (default)
}
```

### Purpose
1. **Object initialization** — set initial state
2. **Code reusability** — same init logic reused for every object
3. **Valid state** — ensures object starts in a consistent state

### Types of Constructors

#### (a) Non-parameterized
```java
Employee() {
    System.out.println("Employee created!");
}
```

#### (b) Parameterized
```java
Employee(String name, int salary) {
    this.employeeName = name;
    this.salary = salary;
}
// Usage
Employee e = new Employee("Raj", 10000);
```
- `this` refers to the current instance — disambiguates parameter vs field

#### (c) Copy Constructor (Manual)
Java has no built-in copy constructor. Must implement manually:
```java
Employee(Employee other) {
    this(other.employeeName, other.salary);  // calling another constructor
}
```

### Constructor Overloading
Multiple constructors with different parameter lists:
```java
Employee() { this("Unknown", 0); }
Employee(String name) { this(name, 0); }
Employee(String name, int salary) { ... }
```

### Constructor Chaining
One constructor calls another via `this()`:
- `this()` must be **first statement** in constructor
- Reduces code duplication
- No limit on chain length

```java
// Without chaining (repetitive)
Employee() { name = "Unknown"; salary = 0; }
Employee(String n) { name = n; salary = 0; }
Employee(String n, int s) { name = n; salary = s; }

// With chaining (clean)
Employee(String n, int s) { name = n; salary = s; }
Employee(String n) { this(n, 0); }
Employee() { this("Unknown", 0); }
```

### Constructor Overloading Advantages
- **Flexibility**: multiple initialization patterns
- **Code Reusability**: same class, different init scenarios
- **Readability**: cleaner grouping of init logic
